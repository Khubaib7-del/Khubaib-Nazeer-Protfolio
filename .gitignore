# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/

# Demo videos — most projects use a LinkedIn embed instead (see main.js).
# bite-balance.mp4 and algo-visualizer.mp4 are NOT allow-listed: they exceed
# GitHub's 100MB file limit and aren't needed anyway (LinkedIn embed covers
# them). pop-rush.mp4 and cognivision.mp4 have no LinkedIn upload, so they're
# pushed directly — both are under the limit (13MB / 92MB).
assets/videos/*.mp4
!assets/videos/pop-rush.mp4
!assets/videos/cognivision.mp4
